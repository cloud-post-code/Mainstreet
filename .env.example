# Optional: Google Maps JavaScript API key for the scroll-driven map background on the home page.
# Get a key at https://console.cloud.google.com/apis/credentials and enable "Maps JavaScript API".
# Restrict the key by HTTP referrer and API to avoid abuse.
# GOOGLE_MAPS_API_KEY=
